{
  "name": "stellarforge",
  "version": "0.1.0",
  "description": "A browser-based incremental space automation game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^7.3.1"
  }
}
